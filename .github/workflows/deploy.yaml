name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t dolkhovykov/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true dolkhovykov/react-test npm test

      - run: docker build -t dolkhovykov/multi-client ./client
      - run: docker build -t dolkhovykov/multi-nginx ./nginx
      - run: docker build -t dolkhovykov/multi-server ./server
      - run: docker build -t dolkhovykov/multi-worker ./worker

      - run: docker push dolkhovykov/multi-client
      - run: docker push dolkhovykov/multi-nginx
      - run: docker push dolkhovykov/multi-server
      - run: docker push dolkhovykov/multi-worker
